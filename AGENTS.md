# 项目说明

## 项目用途

- 本项目是供项目所有者个人观看视频使用的软件。
- 功能设计和技术选型应优先满足个人使用场景，保持简单、稳定、易维护。

## 部署与可移植性

- 项目采用 Docker 模式运行和部署。
- 新增或修改功能时，应确保项目能够通过 Docker 构建并启动。
- 运行所需的服务、依赖和配置应尽量容器化，避免依赖宿主机上的特定开发环境。
- 配置项应通过环境变量或可挂载的配置文件提供，不应硬编码与单台电脑绑定的路径或参数。
- 视频文件、数据库和其他需要保留的数据应使用 Docker 卷或宿主机目录挂载，以便迁移和备份。
- 项目应方便迁移到其他电脑；迁移后原则上只需安装 Docker、复制项目与持久化数据、填写环境配置即可启动。
- 优先提供 `compose.yaml`（或兼容的 Docker Compose 配置）作为统一启动入口。

## 国内镜像与资源约束

- 项目构建、开发和运行过程中，所有可替换的外部资源必须使用中国大陆可稳定访问的国内镜像源，不使用国外官方源。
- Docker 基础镜像和容器依赖优先使用国内容器镜像站；不得直接引用 Docker Hub 的 `docker.io` 或省略镜像仓库地址。
- npm、pnpm、Yarn 等前端包管理器统一使用国内 npm 镜像；锁文件中的依赖下载地址也应指向国内镜像。
- pip、Poetry 等 Python 包管理器统一使用国内 PyPI 镜像。
- 系统软件包、字体、脚本、CDN 静态资源及其他可下载资源均应选择国内镜像或随项目本地化，不应在运行时依赖境外 CDN。
- 新增第三方服务或资源前，应先确认其在中国大陆网络环境中可稳定访问；不能满足时应提供国内替代方案或将资源打包到项目中。
- 国内镜像地址应集中配置或通过构建参数提供，确保镜像失效时可以方便替换，但默认值必须是国内源。
